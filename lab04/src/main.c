int main()
{
	int radius = 40;		//radius variable
	float pi = 3.14f;		//pi number
	char command = 'l';
		if (command == 'l') {		//rozgaluzhennya
			float l = 2 * pi * radius;
} else if (command == 's') {
			float s = pi * (radius * radius);
} else if (command == 'v') {
			float v = (4 / 3) * pi * (radius * radius * radius);
}
	return 0;
}
